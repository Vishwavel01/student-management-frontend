export interface Student {
  rollNo?: number;
  firstName: string;
  lastName: string;
  phoneNo: string;
  email: string;
  department: string;
}

export interface ApiResponse {
  message: string;
}
